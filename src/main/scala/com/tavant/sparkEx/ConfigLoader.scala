package com.tavant.sparkEx
package com.util

import scala.io.Source
object ConfigLoader {/*

  def load(path: String) = {
    val json = Source.fromFile(path)
    // parse
    val mapper: Any = new ObjectMapper() with ScalaObjectMapper
    mapper.registerModule(DefaultScalaModule)
    val parsedJson = mapper.readValue[Map[String, String]](json.reader())
    //    println(parsedJson)
    parsedJson
  }*/
}
